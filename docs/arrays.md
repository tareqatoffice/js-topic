## Arrays

Ordered collections with powerful methods.

```javascript
const a = [1, 2, 3];
a.push(4); a.pop();
a.unshift(0); a.shift();

for (const v of a) console.log(v);
```

Common methods: `map`, `filter`, `reduce`, `find`, `some`, `every`, `slice`, `splice`, `sort`.
